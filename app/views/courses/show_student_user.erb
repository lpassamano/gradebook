<h3><%= @course.name %></h3>

<table>
  <thead>
    <tr>
      <th>
        Assessment
      </th>
      <th>
        Grade
      </th>
      <th>
        Comments
      </th>
    </tr>
  </thead>
  <tbody>
    <% @course.assessments. each do |assessment| %>
      <tr>
        <td>
          <%= assessment.name %>
        </td>
        <td>
          <%= current_user.grades.find {|grade| grade.assessment == assessment}.score %>
        </td>
        <td>
          <%= current_user.grades.find {|grade| grade.assessment == assessment}.comment %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
